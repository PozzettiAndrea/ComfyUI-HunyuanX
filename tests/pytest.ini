[pytest]
markers =
  meshcraft: mark as meshcraft workflow test (deselect with '-m "not meshcraft"')
  slow: marks tests as slow (deselect with '-m "not slow"')
testpaths = .
addopts = -s --import-mode=importlib
pythonpath = .
